# Electron应用标题栏和窗口控制按钮问题解决过程

## 问题描述

在开发BiliSonic应用过程中，我们遇到了以下问题：

1. 应用窗口标题显示为"Electron"而不是"BiliSonic"
2. 窗口控制按钮（最小化、最大化、关闭）与自定义标题栏重叠
3. macOS Dock中显示的应用名称不正确

## 问题分析

经过分析，我们发现问题的根源在于：

1. HTML文档中的`<title>`标签仍然是默认的"Electron"
2. 窗口配置中的`frame`和`titleBarStyle`设置不正确
3. 自定义标题栏与系统标题栏同时存在并重叠
4. 应用名称设置的时机和方式不正确

## 解决步骤

### 1. 修正HTML文档标题

在`app/src/renderer/index.html`文件中，将：

```html
<title>Electron</title>
```

修改为：

```html
<title>BiliSonic</title>
```

这是最关键的一步，因为Electron应用在加载HTML页面时会优先使用HTML中的标题。

### 2. 正确配置窗口框架和标题栏样式

在`app/src/main/index.ts`文件中，修改窗口创建配置：

```typescript
const mainWindow = new BrowserWindow({
  width: 1440,
  height: 768,
  show: false,
  autoHideMenuBar: true,
  // 显示系统标题栏
  frame: true, // 使用系统窗口框架
  titleBarStyle: 'default', // 使用默认标题栏样式，在所有平台上显示完整标题栏
  title: 'BiliSonic', // 设置窗口标题
  // ...其他配置
})
```

### 3. 多重保障的标题设置

为确保标题在各种情况下都能正确显示，我们在多个地方设置了标题：

```typescript
// 设置应用名称
app.name = 'BiliSonic'

// 创建窗口后立即设置标题
mainWindow.setTitle('BiliSonic')

// 在窗口准备显示时再次设置标题
mainWindow.on('ready-to-show', () => {
  mainWindow.show()
  // 再次确保标题设置正确
  mainWindow.setTitle('BiliSonic')
})
```

### 4. 调整自定义标题栏样式

修改`app/src/renderer/src/components/TitleBar/TitleBar.css`文件，确保自定义标题栏不会与系统标题栏冲突：

```css
.title-bar {
  /* ...其他样式 */
  -webkit-app-region: no-drag; /* 不再作为拖拽区域，避免与系统标题栏冲突 */
}

/* 针对macOS的特殊样式 */
@media screen and (-webkit-device-pixel-ratio: 1.5), screen and (device-pixel-ratio: 1.5) {
  .title-bar {
    padding-left: 0; /* 不再需要为窗口控制按钮留出空间 */
  }
}
```

### 5. 确保package.json配置正确

在`app/package.json`文件中，确保设置了正确的应用名称：

```json
{
  "name": "bilisonic",
  "version": "1.0.0",
  "description": "BiliSonic - 哔哩哔哩音频下载工具",
  "productName": "BiliSonic",
  // ...其他配置
}
```

## 测试验证

通过将标题设置为"BiliSonic-1"、"BiliSonic-2"、"BiliSonic-3"等进行测试，确认标题显示正常，窗口控制按钮位置正确，没有重叠问题。

## 经验总结

1. **HTML标题优先级高**：Electron应用会优先使用HTML文档中的`<title>`标签内容作为窗口标题
2. **多重设置保障**：在应用生命周期的不同阶段设置标题，可以确保标题始终正确显示
3. **避免自定义与系统冲突**：当使用系统标题栏时，需要确保自定义UI元素不会与之重叠或冲突
4. **平台特定配置**：针对不同平台(macOS/Windows/Linux)可能需要不同的配置
5. **及时测试验证**：通过修改标题进行测试，可以快速验证问题是否解决

这个问题的解决过程展示了在Electron应用开发中处理UI和窗口配置问题的系统方法，以及如何在多个层面（HTML、主进程、渲染进程）协调工作以实现一致的用户体验。 